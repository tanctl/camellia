(* Simple arithmetic circuit with constraints *)
circuit SimpleCircuit {
  inputs: x, y                   (* public inputs *)
  
  (* compute basic operations *)
  sum = x + y
  product = x * y
  
  (* assert specific values *)
  assert sum == 10              (* sum must equal 10 *)
  assert product == 21          (* product must equal 21 *)
}